# Usando o plotly para criar gráficos mais interativos, de acordo com o site oficial: https://plot.ly/ggplot2/
library(ggplot2)
library(plotly)

pl <-ggplot(mtcars,aes(mpg,wt)) + geom_point()

gpl <- ggplotly(pl)

print(gpl)
